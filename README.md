# depshield-test-repo

Test repository for DepShield dependency migration agent.

## Dependencies
- `request` - HTTP client (deprecated)
- `node-uuid` - UUID generation (deprecated)
- `moment` - Date handling (legacy)
- `lodash` - Utility library
- `axios` - HTTP client (old version with CVEs)